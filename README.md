# RegExp
学习一下正则表达式

又给自己挖了个坑啊，之前写了几个文档，但是都没做完，我保证最近赶紧把之前的坑填完，这是最近的最后一个坑。今天就写下学习正则中的笔记把。

新建正则有两种方式，一种是使用字面量： /abc/   第二种是是使用RegExp构造： new RegExp('abc')  [很明显第一种方法简单些]

/abc/.test()  返回布尔值，表示当前模式能否匹配参数字符串  eg: /tu/.test('liangtuzi') true
/abc/.exec()  返回数组，如果发现匹配就返回一个数组，没有就返回NULL

